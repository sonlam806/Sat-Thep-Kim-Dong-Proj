doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    title Sắt Thép Kim Đồng
    script(src='js/jquery.js')
    script(src='/socket.io/socket.io.js')
    script.
      var socket = io.connect('http://localhost:3000');
      socket.on('news', function (data) {
      document.getElementById('data').innerHTML = data.hello;
      });
  body
    #data

